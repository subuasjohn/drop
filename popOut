<style>
.fullscreen-overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100vw; height: 100vh;
  background: black;
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}
.fullscreen-overlay video {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
}
</style>

<script>
function popInPlace(videoId) {
  const vid = document.getElementById(videoId);
  const overlay = document.createElement('div');
  overlay.className = 'fullscreen-overlay';

  // Move the video into the overlay
  overlay.appendChild(vid);
  document.body.appendChild(overlay);

  // Click anywhere to close overlay
  overlay.addEventListener('click', () => {
    // Put video back in its section
    const section = document.querySelector(`#${videoId}`).closest('.section.video');
    section.querySelector('div').insertBefore(vid, section.querySelector('div').firstChild);
    overlay.remove();
  });
}
</script>



<button class="btn-yellow" onclick="popInPlace('rgb_feed')">Pop-Out</button>



You can make it occupy the monitor area (not just the tab) with:

vid.requestFullscreen();


so it acts like YouTube’s fullscreen player.